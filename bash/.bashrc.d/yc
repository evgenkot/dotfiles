# Yandex Cloud CLI
if [ -f "$HOME/yandex-cloud/path.bash.inc" ]; then
	source "$HOME/yandex-cloud/path.bash.inc"
fi

if [ -f "$HOME/yandex-cloud/completion.bash.inc" ]; then
	source "$HOME/yandex-cloud/completion.bash.inc"
fi
